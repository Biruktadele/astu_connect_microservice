[project]
name = "api-gateway"
version = "0.1.0"
description = "ASTU Connect API Gateway"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-jose[cryptography]",
    "pydantic-settings",
    "httpx",
]
